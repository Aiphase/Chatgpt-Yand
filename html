<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chat Aiphase v.1.2</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #f0f2f5;
      display: flex;
      flex-direction: column;
      height: 100vh;
    }
    .chat-container {
      display: flex;
      flex-direction: column;
      height: 500px;
      max-width: 800px;
      margin: auto;
      border: 1px solid #ddd;
      border-radius: 8px;
      background-color: white;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    .chat-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px 20px;
      background-color: #0078d4;
      color: white;
      font-size: 18px;
      font-weight: bold;
      border-top-left-radius: 8px;
      border-top-right-radius: 8px;
    }
    .header-buttons {
      display: flex;
      gap: 10px;
    }
    .header-button {
      padding: 5px 10px;
      background-color: white;
      color: #0078d4;
      border: 1px solid white;
      border-radius: 4px;
      cursor: pointer;
      font-size: 14px;
    }
    .header-button:hover {
      background-color: #005fa3;
      color: white;
    }
    .chat-messages {
      flex-grow: 1;
      padding: 20px;
      overflow-y: auto;
      display: flex;
      flex-direction: column;
      gap: 15px;
    }
    .message {
      max-width: 70%;
      padding: 10px 15px;
      border-radius: 12px;
      font-size: 14px;
      line-height: 1.5;
    }
    .user-message {
      align-self: flex-end;
      background-color: #0078d4;
      color: white;
    }
    .assistant-message {
      align-self: flex-start;
      background-color: #e9ecef;
      color: #333;
    }
    .chat-input-container {
      padding: 10px 20px;
      display: flex;
      border-top: 1px solid #ddd;
    }
    .chat-input {
      flex-grow: 1;
      padding: 10px 15px;
      font-size: 14px;
      border: 1px solid #ddd;
      border-radius: 20px;
      outline: none;
    }
    .send-button {
      padding: 0 15px;
      margin-left: 10px;
      background-color: #0078d4;
      color: white;
      border: none;
      border-radius: 20px;
      font-size: 14px;
      cursor: pointer;
    }
    .send-button:hover {
      background-color: #005fa3;
    }
  </style>
</head>
<body>
  <div class="chat-container">
    <div class="chat-header">
      <span>ChatGPT Aiphase версия 1.2</span>
      <div class="header-buttons">
        <button class="header-button" onclick="downloadChat()">Скачать</button>
        <button class="header-button" onclick="clearChat()">Очистить</button>
      </div>
    </div>
    <div class="chat-messages" id="chatMessages">
      <div class="message assistant-message">
        Привет! Чем я могу помочь вам сегодня?
      </div>
    </div>
    <div class="chat-input-container">
      <input type="text" class="chat-input" id="chatInput" placeholder="Введите ваше сообщение здесь...">
      <button class="send-button" onclick="sendMessage()">Отправить</button>
    </div>
  </div>

  <script>
async function sendMessage() {
  const chatMessages = document.getElementById('chatMessages');
  const chatInput = document.getElementById('chatInput');
  const userMessage = chatInput.value.trim();

  if (userMessage) {
    // Отображение сообщения пользователя
    const userMessageElement = document.createElement('div');
    userMessageElement.className = 'message user-message';
    userMessageElement.textContent = userMessage;
    chatMessages.appendChild(userMessageElement);

    // Очистка поля ввода
    chatInput.value = '';

    try {
      console.log('Отправка запроса к API:', userMessage);

      // Отправка запроса к серверу
      const response = await fetch('https://version-1-2-runetcensor.amvera.io/',  {
          
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ text: userMessage }),
      });

      if (!response.ok) {
        throw new Error(`Ошибка: ${response.status} ${response.statusText}`);
      }

      const data = await response.json();

      // Обработка ответа
      if (data.reply) {
        const assistantMessageElement = document.createElement('div');
        assistantMessageElement.className = 'message assistant-message';
        assistantMessageElement.textContent = data.reply;
        chatMessages.appendChild(assistantMessageElement);
      } else if (data.error) {
        const errorElement = document.createElement('div');
        errorElement.className = 'message assistant-message';
        errorElement.textContent = `Ошибка: ${data.error}`;
        chatMessages.appendChild(errorElement);
      }
    } catch (error) {
      console.error('Произошла ошибка:', error);
      const errorElement = document.createElement('div');
      errorElement.className = 'message assistant-message';
      errorElement.textContent = `Ошибка: ${error.message}`;
      chatMessages.appendChild(errorElement);
    }

    // Прокрутка к последнему сообщению
    chatMessages.scrollTop = chatMessages.scrollHeight;
  }
}

    function clearChat() {
      const chatMessages = document.getElementById('chatMessages');
      chatMessages.innerHTML = '';
    }

    function downloadChat() {
      const chatMessages = document.getElementById('chatMessages');
      const messages = [...chatMessages.children].map(msg => msg.textContent).join('\n');
      const blob = new Blob([messages], { type: 'text/plain' });
      const link = document.createElement('a');
      link.href = URL.createObjectURL(blob);
      link.download = 'chat.txt';
      link.click();
    }
  </script>
</body>
</html>
